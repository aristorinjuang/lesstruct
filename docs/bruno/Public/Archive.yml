info:
  name: Archive
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/archive"
  params:
    - name: post_type
      value: "post"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
