info:
  name: Delete Own Comment
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/my-comments/{{COMMENT_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
