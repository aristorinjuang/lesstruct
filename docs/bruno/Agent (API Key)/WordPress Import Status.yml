info:
  name: WordPress Import Status
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/wordpress/import/status/{{JOB_ID}}"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
