info:
  name: Get Content
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/content_items/{{CONTENT_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
