info:
  name: Generate Static Site
  type: http
  seq: 16

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/ssg"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
