info:
  name: Get Comments
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/content_items/{{SLUG}}/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
