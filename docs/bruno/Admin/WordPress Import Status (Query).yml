info:
  name: WordPress Import Status (Query)
  type: http
  seq: 19

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/wordpress/import/status"
  params:
    - name: jobId
      value: "{{JOB_ID}}"
      type: query
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
