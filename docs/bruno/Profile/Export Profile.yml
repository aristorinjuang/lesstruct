info:
  name: Export Profile
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/profile/export"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
