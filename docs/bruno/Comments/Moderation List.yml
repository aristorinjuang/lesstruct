info:
  name: Moderation List
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/content_items/{{CONTENT_ID}}/comments"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
