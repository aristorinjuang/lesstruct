info:
  name: WordPress Import Status
  type: http
  seq: 18

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/wordpress/import/status/{{JOB_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
