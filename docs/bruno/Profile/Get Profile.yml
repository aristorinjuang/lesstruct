info:
  name: Get Profile
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/profile"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
