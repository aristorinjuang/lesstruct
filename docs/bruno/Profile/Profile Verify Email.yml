info:
  name: Profile Verify Email
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/profile/verify-email?token={{PROFILE_VERIFICATION_TOKEN}}"
  params:
    - name: token
      value: "{{PROFILE_VERIFICATION_TOKEN}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
