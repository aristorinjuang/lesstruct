info:
  name: Soft Delete User
  type: http
  seq: 9

http:
  method: POST
  url: "{{BASE_URL}}/api/admin/users/{{USER_ID}}/soft-delete"
  body:
    type: json
    data: |-
      {
        "confirmed": true,
        "reason": "testing purpose"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
