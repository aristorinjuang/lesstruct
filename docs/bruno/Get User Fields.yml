info:
  name: Get User Fields (Admin)
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/user_fields"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
