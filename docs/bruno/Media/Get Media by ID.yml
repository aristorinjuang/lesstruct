info:
  name: Get Media by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/media/{{MEDIA_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
