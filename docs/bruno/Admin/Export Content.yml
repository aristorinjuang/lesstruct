info:
  name: Export Content
  type: http
  seq: 23

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/export"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
