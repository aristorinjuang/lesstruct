info:
  name: Dashboard
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/dashboard/stats"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
