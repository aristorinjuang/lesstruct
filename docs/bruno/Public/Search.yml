info:
  name: Search
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/search?q={{SEARCH_QUERY}}"
  params:
    - name: q
      value: "{{SEARCH_QUERY}}"
      type: query
    - name: limit
      value: "10"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
