info:
  name: Hugo Import Status (Query)
  type: http
  seq: 22

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/hugo/import/status"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
