info:
  name: Hugo Import Status
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/hugo/import/status/{{JOB_ID}}"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
