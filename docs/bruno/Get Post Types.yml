info:
  name: Get Post Types
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/post_types"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
