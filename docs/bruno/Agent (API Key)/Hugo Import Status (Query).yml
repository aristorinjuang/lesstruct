info:
  name: Hugo Import Status (Query)
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/hugo/import/status"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
