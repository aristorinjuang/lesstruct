info:
  name: Export Content
  type: http
  seq: 15

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/export"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
