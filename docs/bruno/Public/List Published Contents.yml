info:
  name: List Published Contents
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/content_items"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
