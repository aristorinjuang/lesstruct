# Override the API base URL when the admin SPA and the API run on different
# origins (e.g. dev mode with a non-default backend port).
# Omit or leave empty for same-origin (production mode) — the SPA follows
# whatever host:port you open in the browser.
#
# VITE_API_BASE_URL=http://localhost:8081
