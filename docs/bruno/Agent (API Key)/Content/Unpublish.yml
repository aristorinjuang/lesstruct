info:
  name: Unpublish
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/content/{{CONTENT_ID}}/unpublish"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
