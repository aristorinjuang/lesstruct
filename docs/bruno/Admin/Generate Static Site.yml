info:
  name: Generate Static Site
  type: http
  seq: 24

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/ssg"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
