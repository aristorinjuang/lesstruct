info:
  name: Approve User
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/admin/users/{{USER_ID}}/approve"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
