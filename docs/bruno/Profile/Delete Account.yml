info:
  name: Delete Account
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{BASE_URL}}/api/profile/account"
  body:
    type: json
    data: |-
      {
        "confirmation": "DELETE"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
