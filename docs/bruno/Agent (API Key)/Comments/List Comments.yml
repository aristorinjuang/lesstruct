info:
  name: List Comments
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/content/{{CONTENT_ID}}/comments"
  auth:
    type: bearer
    token: "{{API_KEY}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
