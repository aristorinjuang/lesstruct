info:
  name: Get User Fields
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/profile/user-fields"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
