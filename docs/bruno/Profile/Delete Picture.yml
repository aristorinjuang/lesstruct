info:
  name: Delete Picture
  type: http
  seq: 10

http:
  method: DELETE
  url: "{{BASE_URL}}/api/profile/picture"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
