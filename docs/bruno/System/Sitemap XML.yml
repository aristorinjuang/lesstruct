info:
  name: Sitemap XML
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/sitemap.xml"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
