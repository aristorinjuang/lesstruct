info:
  name: User Deleted Content
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/users/{{USER_ID}}/deleted-content"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
