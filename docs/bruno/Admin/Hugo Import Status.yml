info:
  name: Hugo Import Status
  type: http
  seq: 21

http:
  method: GET
  url: "{{BASE_URL}}/api/admin/hugo/import/status/{{JOB_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
