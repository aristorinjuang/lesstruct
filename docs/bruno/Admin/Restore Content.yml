info:
  name: Restore Content
  type: http
  seq: 13

http:
  method: POST
  url: "{{BASE_URL}}/api/admin/content/{{CONTENT_ID}}/restore"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
