info:
  name: Get Author
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/authors/{{AUTHOR_USERNAME}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
