info:
  name: Pending
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/comments/pending"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
