info:
  name: Get Published Content
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/public/content_items/{{SLUG}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
