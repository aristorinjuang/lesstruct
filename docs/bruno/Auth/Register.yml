info:
  name: Register
  type: http
  seq: 5

# Returns 403 REGISTRATION_DISABLED when [comments] enabled = false in
# config.toml — self-registration only creates Commentator accounts.

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "username": "{{USERNAME}}",
        "email": "{{USER_EMAIL}}",
        "password": "{{USER_PASSWORD}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
