info:
  name: Get SEO
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/v1/content_items/{{CONTENT_ID}}/seo"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
