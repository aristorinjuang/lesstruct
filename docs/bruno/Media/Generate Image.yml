info:
  name: Generate Image
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/v1/media/generate"
  body:
    type: json
    data: |-
      {
        "prompt": "A beautiful sunset over the ocean"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
