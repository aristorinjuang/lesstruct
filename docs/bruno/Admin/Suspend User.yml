info:
  name: Suspend User
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/api/admin/users/{{USER_ID}}/suspend"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
