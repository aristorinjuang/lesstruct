info:
  name: Delete Media
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{BASE_URL}}/api/v1/media/{{MEDIA_ID}}"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
