info:
  name: Reject User
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/api/admin/users/{{USER_ID}}/reject"
  body:
    type: json
    data: |-
      {
        "confirmed": true
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
